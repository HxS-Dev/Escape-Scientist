import React from 'react';
import { shallow } from 'enzyme';
import Home from './Home';

describe('First test Example', () => {
    it('1 should not be 3', () => {
        expect(1).toBe(3);
    });
});